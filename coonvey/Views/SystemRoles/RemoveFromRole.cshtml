@model coonvey.ViewModels.ChangeUserRoleViewModel

@{
    ViewBag.Title = "Remove User From Role";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<h2>@ViewBag.Title</h2>

<div class="row">
    <h3>Are you sure you want to remove user from the selected role?</h3>
    <div>
        <dl class="dl-horizontal">

            <dt>
                @Html.DisplayNameFor(model => model.UserName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.UserName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.RoleInUse)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.RoleInUse)
            </dd>

        </dl>

        @using (Html.BeginForm())
            {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Remove" class="btn btn-primary" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>

</div>